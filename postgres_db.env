# This file defines environment variables which control how the database for
# the ngenCERF app is configured.
# Only used by compose.yaml for development

## PostgreSQL variables ##
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
