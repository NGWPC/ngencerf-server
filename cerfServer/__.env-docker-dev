# This is the base environment file for development (when running in Docker) which is copied to cerfServer/.env in the Docker file
# This file is source-controlled.  No deployment-specific data should be in here.
# The only values in here are those values which are static and the same regardless of deployment

CERF_SERVER_SQL_LOGGING=False

# SECURITY WARNING: don't run with debug turned on in production!
DJANGO_DEBUG=True

NGEN_ENVIRONMENT=DOCKER

ENTERPRISE_DATA_URL='http://hydroapi.test.nextgenwaterprediction.com:8000'

REDIS_URL=redis://redis:6379/1

# ---------------------------
# Cluster specific data below which should be defined in the deployment-specific .env-override
# ----------------------------

# For development, it's ok if all deploymnets use the same secret-key.  No need to override
CERF_SERVER_SECRET_KEY='django-insecure-gmuchz(a93zw!8+75r+2%nx)mxvz*m(z9%%+i(#t6$1cg!8cg8'

# Note: Not sure how this can be specified.  We need an .env-override file for each user.
# For development you must define your own s3 directory
# for example, NGENCERF_ARCHIVE_S3_PATH=s3://ngwpc-dev/peter.kronenberg/ngencerf_archive/
# It is also important to create a dummy file in the directory since empty directories on S3 are ephemeral.  See README
NGENCERF_ARCHIVE_S3_PATH=