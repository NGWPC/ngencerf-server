-- Disconnect a user from Active Directory for testing.
-- This removes the AD identity link but keeps the Django user.

UPDATE custom_user
SET
    ad_guid = NULL,
    last_ldap_sync = NULL
WHERE email = '<email>';