# ngencerf CLI

A command-line tool for managing calibration and validation workflows in the ngen CERF environment.

## 📦 Installation

First, create and activate a virtual environment:

```bash
cd cli
python -m venv .venv
source .venv/bin/activate
