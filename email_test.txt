from django.core.mail import EmailMessage
from django.conf import settings

email = EmailMessage(
    subject="Test email",
    body="SMTP working",
    from_email=settings.DEFAULT_FROM_EMAIL,
    to=["pakronenberg+ngen@gmail.com"],
)

email.send(fail_silently=False)