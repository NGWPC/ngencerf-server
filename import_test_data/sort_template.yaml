# ─────────────────────────────
# Calibration Job Sorting Template
# ─────────────────────────────
# Leave blank for default sorting (descending by job ID).
# The backend will use "-id" if no sort is provided.
#
# ────────────────
# VALID FIELD OPTIONS (case-sensitive)
# ────────────────
# Common sort fields available for calibration jobs:
#   id
#   gage_id
#   status
#   user_formulation_name
#   calibration_start_period
#   calibration_end_period
#   submit_date
#   created_at
#   objective_function
#   optimization_algorithm
#   stop_criteria
#   job_genesis
#   archived
#
# ────────────────
# VALID DIRECTION VALUES
# ────────────────
#   asc   → ascending order
#   desc  → descending order
#
# ────────────────
# EXAMPLES
# ────────────────
#   field: submit_date
#   direction: desc
#
#   field: gage_id
#   direction: asc
#
#   field: objective_function
#   direction: asc
#
#   field: calibration_end_period
#   direction: desc

field: ""        # One of the valid field names listed above
direction: ""    # "asc" or "desc"
